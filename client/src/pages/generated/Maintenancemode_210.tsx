// AUTO-GENERATED DRAFT SCREEN: MaintenanceMode

import React, { useState, useEffect, useCallback } from 'react';
import { Button } from './ui/button'; // Assuming shadcn/ui Button component

// Mock tRPC hook for demonstration purposes
// In a real application, this would be generated by tRPC
const useMaintenanceStatus = () => {
  const [status, setStatus] = useState<'loading' | 'maintenance' | 'operational'>('loading');
  const [error, setError] = useState<string | null>(null);

  useEffect(() => {
    const timer = setTimeout(() => {
      // Simulate API call delay
      const isMaintenance = Math.random() > 0.5; // Simulate random maintenance status
      if (isMaintenance) {
        setStatus('maintenance');
        setError(null);
      } else {
        setStatus('operational');
        setError('Failed to fetch maintenance status. Please try again.');
      }
    }, 2000);

    return () => clearTimeout(timer);
  }, []);

  const refetch = useCallback(() => {
    setStatus('loading');
    setError(null);
    // Simulate refetch logic
    const timer = setTimeout(() => {
      const isMaintenance = Math.random() > 0.5; // Simulate random maintenance status
      if (isMaintenance) {
        setStatus('maintenance');
        setError(null);
      } else {
        setStatus('operational');
        setError('Failed to fetch maintenance status. Please try again.');
      }
    }, 1500);
    return () => clearTimeout(timer);
  }, []);

  return { status, error, refetch };
};

interface MaintenanceModeProps {
  appName?: string;
  supportEmail?: string;
}

const MaintenanceMode: React.FC<MaintenanceModeProps> = ({
  appName = 'SKYCOIN4444',
  supportEmail = 'support@skycoin4444.com',
}) => {
  const { status, error, refetch } = useMaintenanceStatus();

  const handleRetry = () => {
    refetch();
  };

  const renderContent = () => {
    switch (status) {
      case 'loading':
        return (
          <div className="flex flex-col items-center">
            <div className="animate-spin rounded-full h-12 w-12 border-4 border-t-4 border-blue-500 dark:border-blue-400 border-opacity-25 mb-4"></div>
            <p className="text-lg">Checking service status...</p>
          </div>
        );
      case 'maintenance':
        return (
          <>
            <svg
              className="mx-auto h-16 w-16 text-yellow-500 dark:text-yellow-400 mb-4"
              fill="none"
              viewBox="0 0 24 24"
              stroke="currentColor"
              aria-hidden="true"
            >
              <path
                strokeLinecap="round"
                strokeLinejoin="round"
                strokeWidth={2}
                d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"
              />
            </svg>
            <h1 className="text-3xl font-bold mb-2">Maintenance Mode</h1>
            <p className="text-lg mb-6">
              Our {appName} services are currently undergoing scheduled maintenance. We apologize for any inconvenience.
            </p>
            <p className="mt-6 text-sm text-gray-500 dark:text-gray-400">
              We expect to be back online shortly. Thank you for your patience.
            </p>
            <p className="mt-4 text-sm text-gray-500 dark:text-gray-400">
              For urgent inquiries, please contact us at{' '}
              <a href={`mailto:${supportEmail}`} className="text-blue-600 dark:text-blue-400 hover:underline">
                {supportEmail}
              </a>
              .
            </p>
          </>
        );
      case 'operational':
        return (
          <div className="flex flex-col items-center">
            <svg
              className="mx-auto h-16 w-16 text-green-500 dark:text-green-400 mb-4"
              fill="none"
              viewBox="0 0 24 24"
              stroke="currentColor"
              aria-hidden="true"
            >
              <path
                strokeLinecap="round"
                strokeLinejoin="round"
                strokeWidth={2}
                d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"
              />
            </svg>
            <h1 className="text-3xl font-bold mb-2">Services Operational</h1>
            <p className="text-lg mb-6">
              Our {appName} services are now fully operational. Thank you for your patience.
            </p>
            <Button onClick={() => window.location.reload()} className="mt-4">
              Refresh Page
            </Button>
          </div>
        );
      default:
        return null;
    }
  };

  return (
    <div className="flex items-center justify-center min-h-screen bg-gray-100 dark:bg-gray-900 text-gray-900 dark:text-gray-100 p-4 font-sans" role="alert" aria-live="polite">
      <div className="bg-white dark:bg-gray-800 shadow-lg rounded-lg p-8 max-w-md w-full text-center border border-gray-200 dark:border-gray-700">
        {renderContent()}

        {error && status !== 'operational' && (
          <div className="mt-6 text-red-500 dark:text-red-400" role="alert">
            <p className="font-medium">Error: {error}</p>
            <Button onClick={handleRetry} className="mt-2" variant="outline">
              Try Again
            </Button>
          </div>
        )}
      </div>
    </div>
  );
};

export default MaintenanceMode;
