// AUTO-GENERATED DRAFT SCREEN: CryptoBankAccountLinking
import { useForm } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import * as z from 'zod';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';
import { Switch } from '@/components/ui/switch';
import { useTheme } from '@/components/theme-provider'; // Assuming a theme provider

// Define the form schema using Zod
const formSchema = z.object({
  bankName: z.string().min(2, { message: 'Bank name must be at least 2 characters.' }),
  accountNumber: z.string().min(5, { message: 'Account number must be at least 5 characters.' }),
  routingNumber: z.string().min(9, { message: 'Routing number must be 9 digits.' }).max(9, { message: 'Routing number must be 9 digits.' }),
  confirmAccount: z.boolean(), // Make it a required boolean
});

type FormValues = z.infer<typeof formSchema>;

// Placeholder for tRPC hooks
// In a real application, these would be generated by tRPC
const trpc = {
  // Mock tRPC context for demonstration
  useContext: () => ({}),
  crypto: {
    linkBankAccount: {
      useMutation: () => ({
        mutate: (data: FormValues) => {
          return new Promise((resolve) => {
            setTimeout(() => {
              console.log('Linking bank account:', data);
              resolve({ success: true });
            }, 2000);
          });
        },
        isLoading: false,
        isError: false,
        error: null as Error | null, // Correctly type error as Error or null
      }),
    },
  },
};

export function CryptoBankAccountLinking() {
  const { theme, setTheme } = useTheme();
  const form = useForm<FormValues>({
    resolver: zodResolver(formSchema),
    defaultValues: {
      bankName: '',
      accountNumber: '',
      routingNumber: '',
      confirmAccount: false, // Provide a default value for the required boolean
    },
  });

  const { mutate, isLoading, isError, error } = trpc.crypto.linkBankAccount.useMutation();

  const onSubmit = (values: FormValues) => {
    mutate(values);
  };

  return (
    <div className="flex items-center justify-center min-h-screen bg-background">
      <Card className="w-[450px]">
        <CardHeader>
          <CardTitle>Link Bank Account</CardTitle>
          <CardDescription>Connect your bank account to SKYCOIN4444.</CardDescription>
        </CardHeader>
        <CardContent>
          <form onSubmit={form.handleSubmit(onSubmit)} className="space-y-6">
            <div className="grid w-full items-center gap-4">
              <div className="flex flex-col space-y-1.5">
                <Label htmlFor="bankName">Bank Name</Label>
                <Input
                  id="bankName"
                  placeholder="e.g., Bank of America"
                  {...form.register('bankName')}
                  aria-invalid={form.formState.errors.bankName ? 'true' : 'false'}
                />
                {form.formState.errors.bankName && (
                  <p className="text-red-500 text-sm">{form.formState.errors.bankName.message}</p>
                )}
              </div>
              <div className="flex flex-col space-y-1.5">
                <Label htmlFor="accountNumber">Account Number</Label>
                <Input
                  id="accountNumber"
                  placeholder="Your bank account number"
                  {...form.register('accountNumber')}
                  aria-invalid={form.formState.errors.accountNumber ? 'true' : 'false'}
                />
                {form.formState.errors.accountNumber && (
                  <p className="text-red-500 text-sm">{form.formState.errors.accountNumber.message}</p>
                )}
              </div>
              <div className="flex flex-col space-y-1.5">
                <Label htmlFor="routingNumber">Routing Number</Label>
                <Input
                  id="routingNumber"
                  placeholder="Your bank routing number"
                  {...form.register('routingNumber')}
                  aria-invalid={form.formState.errors.routingNumber ? 'true' : 'false'}
                />
                {form.formState.errors.routingNumber && (
                  <p className="text-red-500 text-sm">{form.formState.errors.routingNumber.message}</p>
                )}
              </div>
              <div className="flex items-center space-x-2">
                <Switch
                  id="confirmAccount"
                  checked={form.watch('confirmAccount')}
                  onCheckedChange={(checked) => form.setValue('confirmAccount', checked)}
                  aria-label="Confirm account details"
                />
                <Label htmlFor="confirmAccount">I confirm the bank account details are correct.</Label>
                {form.formState.errors.confirmAccount && (
                  <p className="text-red-500 text-sm">{form.formState.errors.confirmAccount.message}</p>
                )}
              </div>
            </div>
            {isError && <p className="text-red-500 text-sm">Error: {error?.message || 'Failed to link account.'}</p>}
            <Button type="submit" className="w-full" disabled={isLoading}>
              {isLoading ? 'Linking...' : 'Link Account'}
            </Button>
          </form>
          <div className="flex justify-end mt-4">
            <Switch
              id="dark-mode-toggle"
              checked={theme === 'dark'}
              onCheckedChange={(checked) => setTheme(checked ? 'dark' : 'light')}
              aria-label="Toggle dark mode"
            />
            <Label htmlFor="dark-mode-toggle" className="ml-2">Dark Mode</Label>
          </div>
        </CardContent>
      </Card>
    </div>
  );
}


export default function Cryptobankaccountlinking_41() { return null; }
