// AUTO-GENERATED DRAFT SCREEN: BeneficiaryManager
import React, { useState, useEffect } from 'react';
import { Card, CardContent, CardHeader, CardTitle } from './ui/card';
import { Button } from './ui/button';
import { Input } from './ui/input';
import { Label } from './ui/label';
import { Loader2 } from 'lucide-react';

interface Beneficiary {
  id: string;
  name: string;
  address: string;
}

// Placeholder for tRPC hook integration
// In a real application, this would be generated by tRPC
const useBeneficiaries = () => {
  const [data, setData] = useState<Beneficiary[] | null>(null);
  const [isLoading, setIsLoading] = useState(true);
  const [error, setError] = useState<Error | null>(null);

  useEffect(() => {
    const fetchData = async () => {
      try {
        // Simulate API call
        await new Promise(resolve => setTimeout(resolve, 1500));
        const mockBeneficiaries: Beneficiary[] = [
          { id: '1', name: 'John Doe', address: '0x123abc...' },
          { id: '2', name: 'Jane Smith', address: '0x456def...' },
          { id: '3', name: 'Crypto Wallet', address: '0x789ghi...' },
        ];
        setData(mockBeneficiaries);
      } catch (err) {
        setError(err as Error);
      } finally {
        setIsLoading(false);
      }
    };
    fetchData();
  }, []);

  return { data, isLoading, error };
};

const BeneficiaryManager: React.FC = () => {
  const { data: beneficiaries, isLoading, error } = useBeneficiaries();

  if (isLoading) {
    return (
      <div className="flex justify-center items-center h-screen" role="status" aria-live="polite">
        <Loader2 className="h-8 w-8 animate-spin" aria-label="Loading beneficiaries" />
        <span className="sr-only">Loading...</span>
      </div>
    );
  }

  if (error) {
    return (
      <div className="flex justify-center items-center h-screen text-red-500" role="alert" aria-live="assertive">
        <p>Error loading beneficiaries: {error.message}</p>
      </div>
    );
  }

  return (
    <div className="container mx-auto p-4 dark:bg-gray-900 dark:text-gray-100 min-h-screen">
      <Card className="w-full max-w-md mx-auto">
        <CardHeader>
          <CardTitle className="text-2xl font-bold">Beneficiary Manager</CardTitle>
        </CardHeader>
        <CardContent>
          <div className="grid gap-4" aria-label="List of beneficiaries">
            {beneficiaries?.map((beneficiary) => (
              <div key={beneficiary.id} className="flex items-center justify-between p-2 border rounded-md dark:border-gray-700">
                <div className="flex-grow mr-4">
                  <Label htmlFor={`name-${beneficiary.id}`} className="font-medium text-gray-700 dark:text-gray-300 sr-only">Beneficiary Name</Label>
                  <p id={`name-${beneficiary.id}`} className="text-lg font-semibold">{beneficiary.name}</p>
                  <Label htmlFor={`address-${beneficiary.id}`} className="font-medium text-gray-700 dark:text-gray-300 sr-only">Beneficiary Address</Label>
                  <Input
                    id={`address-${beneficiary.id}`}
                    value={beneficiary.address}
                    readOnly
                    className="mt-1 block w-full text-sm text-gray-500 dark:text-gray-400 bg-gray-50 dark:bg-gray-800 border-gray-300 dark:border-gray-600 rounded-md"
                    aria-labelledby={`name-${beneficiary.id}`}
                  />
                </div>
                <Button variant="outline" aria-label={`Edit ${beneficiary.name}`}>Edit</Button>
              </div>
            ))}
            <Button className="w-full mt-4" aria-label="Add a new beneficiary">Add New Beneficiary</Button>
          </div>
        </CardContent>
      </Card>
    </div>
  );
};

export default BeneficiaryManager;
