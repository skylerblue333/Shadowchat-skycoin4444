// AUTO-GENERATED DRAFT SCREEN: RewardCenter
import React from 'react';
import { useQuery } from '@tanstack/react-query';
import { Button } from '@/components/ui/button';
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';
import { Skeleton } from '@/components/ui/skeleton';
import { Alert, AlertDescription, AlertTitle } from '@/components/ui/alert';
import { Terminal } from 'lucide-react'; // Example icon for Alert

// --- Types --- //
interface Reward {
  id: string;
  name: string;
  points: number;
  image: string;
  description: string;
}

// --- Mock tRPC Setup (for demonstration) --- //
// In a real application, this would be generated by tRPC
const trpc = {
  rewards: {
    getRewards: {
      useQuery: () => useQuery<Reward[], Error>({
        queryKey: ['rewards'],
        queryFn: async () => {
          // Simulate API call delay
          await new Promise(resolve => setTimeout(resolve, 1500));
          // Simulate potential error
          // if (Math.random() > 0.7) throw new Error('Failed to fetch rewards. Please try again.');
          return [
            { id: '1', name: '100 SKYCOIN', points: 100, image: 'https://via.placeholder.com/150/FFD700/000000?text=100+Coins', description: 'Redeem for 100 in-game SKYCOIN.' },
            { id: '2', name: 'Exclusive Avatar', points: 500, image: 'https://via.placeholder.com/150/8A2BE2/FFFFFF?text=Avatar', description: 'Unlock a unique player avatar.' },
            { id: '3', name: 'XP Boost (1 Hour)', points: 200, image: 'https://via.placeholder.com/150/00CED1/FFFFFF?text=XP+Boost', description: 'Gain double XP for one hour.' },
            { id: '4', name: 'Mystery Crate', points: 750, image: 'https://via.placeholder.com/150/A9A9A9/FFFFFF?text=Crate', description: 'Contains random rare items.' },
            { id: '5', name: 'Premium Pass (7 Days)', points: 1000, image: 'https://via.placeholder.com/150/FF6347/FFFFFF?text=Premium', description: 'Enjoy premium features for 7 days.' },
          ];
        },
      }),
    },
  },
};

// --- RewardCenter Component --- //
const RewardCenter: React.FC = () => {
  const { data: rewards, isLoading, isError, error } = trpc.rewards.getRewards.useQuery();

  return (
    <div className="container mx-auto p-4 sm:p-6 lg:p-8 bg-background text-foreground min-h-screen">
      <h1 className="text-4xl font-extrabold mb-8 text-center tracking-tight lg:text-5xl">
        <span className="bg-gradient-to-r from-purple-400 to-pink-600 text-transparent bg-clip-text">Reward Center</span>
      </h1>

      {isLoading && (
        <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6">
          {[...Array(4)].map((_, i) => (
            <Card key={i} className="w-full animate-pulse">
              <CardHeader>
                <Skeleton className="h-6 w-3/4 mb-2" />
                <Skeleton className="h-4 w-1/2" />
              </CardHeader>
              <CardContent className="space-y-4">
                <Skeleton className="h-40 w-full rounded-md" />
                <Skeleton className="h-10 w-full" />
              </CardContent>
            </Card>
          ))}
        </div>
      )}

      {isError && (
        <Alert variant="destructive" className="max-w-md mx-auto">
          <Terminal className="h-4 w-4" />
          <AlertTitle>Error</AlertTitle>
          <AlertDescription>
            Failed to load rewards: {error?.message || 'Unknown error'}. Please try again later.
          </AlertDescription>
        </Alert>
      )}

      {!isLoading && !isError && rewards && rewards.length > 0 && (
        <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6">
          {rewards.map((reward) => (
            <Card key={reward.id} className="w-full flex flex-col transition-all hover:shadow-lg dark:hover:shadow-purple-500/30">
              <CardHeader className="pb-2">
                <CardTitle className="text-2xl font-semibold text-primary">{reward.name}</CardTitle>
                <CardDescription className="text-muted-foreground text-lg">{reward.points} points</CardDescription>
              </CardHeader>
              <CardContent className="flex-grow flex flex-col justify-between p-4 pt-0">
                <div className="relative w-full h-40 mb-4 rounded-md overflow-hidden">
                  <img
                    src={reward.image}
                    alt={reward.name}
                    className="absolute inset-0 w-full h-full object-cover transition-transform duration-300 hover:scale-105"
                  />
                </div>
                <p className="text-sm text-gray-600 dark:text-gray-400 mb-4 flex-grow">{reward.description}</p>
                <Button className="w-full" aria-label={`Redeem ${reward.name}`}>
                  Redeem
                </Button>
              </CardContent>
            </Card>
          ))}
        </div>
      )}

      {!isLoading && !isError && rewards && rewards.length === 0 && (
        <div className="text-center py-10">
          <p className="text-xl text-muted-foreground">No rewards available at the moment. Check back later!</p>
        </div>
      )}
    </div>
  );
};

export default RewardCenter;
