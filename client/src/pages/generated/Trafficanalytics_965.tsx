// AUTO-GENERATED DRAFT SCREEN: TrafficAnalytics
import React, { useState, useEffect, useCallback } from 'react';
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { Skeleton } from "@/components/ui/skeleton";
import { Alert, AlertDescription, AlertTitle } from "@/components/ui/alert";
import { Terminal, RefreshCw } from "lucide-react";

// --- Type Definitions ---
interface TrafficData {
  date: string;
  views: number;
  uniqueVisitors: number;
  bounceRate: number; // as a percentage, e.g., 0.35 for 35%
  avgSessionDuration: number; // in seconds
}

interface TrafficAnalyticsProps {
  // No specific props for now, data will be fetched internally
}

// --- Mock tRPC Hook (simulating data fetching) ---
// In a real application, this would be generated by tRPC client and connect to a backend.
const useTrafficAnalyticsData = () => {
  const [data, setData] = useState<TrafficData[]>([]);
  const [isLoading, setIsLoading] = useState(true);
  const [error, setError] = useState<string | null>(null);

  const fetchData = useCallback(async () => {
    try {
      setIsLoading(true);
      setError(null);
      // Simulate API call delay
      await new Promise(resolve => setTimeout(resolve, 1500));

      // Simulate successful data fetch
      const mockData: TrafficData[] = [
        { date: '2026-05-01', views: 1200, uniqueVisitors: 800, bounceRate: 0.35, avgSessionDuration: 210 },
        { date: '2026-05-02', views: 1350, uniqueVisitors: 850, bounceRate: 0.32, avgSessionDuration: 225 },
        { date: '2026-05-03', views: 1100, uniqueVisitors: 750, bounceRate: 0.38, avgSessionDuration: 190 },
        { date: '2026-05-04', views: 1500, uniqueVisitors: 900, bounceRate: 0.30, avgSessionDuration: 240 },
        { date: '2026-05-05', views: 1400, uniqueVisitors: 880, bounceRate: 0.33, avgSessionDuration: 215 },
        { date: '2026-05-06', views: 1600, uniqueVisitors: 950, bounceRate: 0.28, avgSessionDuration: 250 },
        { date: '2026-05-07', views: 1450, uniqueVisitors: 890, bounceRate: 0.31, avgSessionDuration: 200 },
      ];
      setData(mockData);
    } catch (err) {
      setError('Failed to load traffic data. Please try again.');
      console.error('Error fetching traffic data:', err);
    } finally {
      setIsLoading(false);
    }
  }, []);

  useEffect(() => {
    fetchData();
  }, [fetchData]);

  return { data, isLoading, error, refetch: fetchData };
};

// --- Helper to format seconds into MM:SS ---
const formatDuration = (seconds: number): string => {
  const minutes = Math.floor(seconds / 60);
  const remainingSeconds = seconds % 60;
  return `${minutes.toString().padStart(2, '0')}:${remainingSeconds.toString().padStart(2, '0')}`;
};

const TrafficAnalytics: React.FC<TrafficAnalyticsProps> = () => {
  const { data, isLoading, error, refetch } = useTrafficAnalyticsData();

  // --- Aggregated Metrics ---
  const totalViews = data.reduce((sum, item) => sum + item.views, 0);
  const totalUniqueVisitors = data.reduce((sum, item) => sum + item.uniqueVisitors, 0);
  const averageBounceRate = data.length > 0 ? (data.reduce((sum, item) => sum + item.bounceRate, 0) / data.length) : 0;
  const averageSessionDuration = data.length > 0 ? (data.reduce((sum, item) => sum + item.avgSessionDuration, 0) / data.length) : 0;

  // --- Loading State ---
  if (isLoading) {
    return (
      <div className="p-4 space-y-4 animate-pulse dark:bg-gray-950 min-h-screen" role="status" aria-live="polite" aria-label="Loading traffic analytics data">
        <Skeleton className="h-10 w-1/3 bg-gray-200 dark:bg-gray-800" />
        <div className="grid gap-4 md:grid-cols-2 lg:grid-cols-4 mb-6">
          <Skeleton className="h-32 w-full bg-gray-200 dark:bg-gray-800" />
          <Skeleton className="h-32 w-full bg-gray-200 dark:bg-gray-800" />
          <Skeleton className="h-32 w-full bg-gray-200 dark:bg-gray-800" />
          <Skeleton className="h-32 w-full bg-gray-200 dark:bg-gray-800" />
        </div>
        <Skeleton className="h-64 w-full bg-gray-200 dark:bg-gray-800" />
        <Skeleton className="h-10 w-32 ml-auto bg-gray-200 dark:bg-gray-800" />
      </div>
    );
  }

  // --- Error State ---
  if (error) {
    return (
      <div className="p-4 dark:bg-gray-950 min-h-screen" role="alert" aria-live="assertive">
        <Alert variant="destructive" className="bg-red-50 dark:bg-red-950 border-red-200 dark:border-red-800 text-red-800 dark:text-red-200">
          <Terminal className="h-4 w-4 text-red-600 dark:text-red-400" />
          <AlertTitle className="font-semibold">Error Loading Data</AlertTitle>
          <AlertDescription>{error}</AlertDescription>
          <Button onClick={refetch} className="mt-4 bg-red-600 hover:bg-red-700 dark:bg-red-700 dark:hover:bg-red-800 text-white">
            <RefreshCw className="mr-2 h-4 w-4" /> Retry
          </Button>
        </Alert>
      </div>
    );
  }

  // --- Main Content ---
  return (
    <div className="p-4 sm:p-6 lg:p-8 dark:bg-gray-950 dark:text-gray-50 min-h-screen font-sans antialiased">
      <h1 className="text-3xl sm:text-4xl font-extrabold mb-6 text-gray-900 dark:text-gray-50" tabIndex={0}>Traffic Analytics</h1>

      <div className="grid gap-4 md:grid-cols-2 lg:grid-cols-4 mb-8" role="region" aria-label="Key traffic metrics">
        <Card className="bg-white dark:bg-gray-800 shadow-sm hover:shadow-md transition-shadow duration-200 ease-in-out border border-gray-200 dark:border-gray-700">
          <CardHeader className="flex flex-row items-center justify-between space-y-0 pb-2">
            <CardTitle className="text-sm font-medium text-gray-600 dark:text-gray-400">Total Views</CardTitle>
            <svg
              xmlns="http://www.w3.org/2000/svg"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              strokeLinecap="round"
              strokeLinejoin="round"
              strokeWidth="2"
              className="h-5 w-5 text-gray-400 dark:text-gray-500"
              aria-hidden="true"
            >
              <path d="M2 12s3-7 10-7 10 7 10 7-3 7-10 7-10-7-10-7Z" />
              <circle cx="12" cy="12" r="3" />
            </svg>
          </CardHeader>
          <CardContent>
            <div className="text-3xl font-bold text-gray-900 dark:text-gray-50">{totalViews.toLocaleString()}</div>
            <p className="text-xs text-green-600 dark:text-green-400">+20.1% from last month</p>
          </CardContent>
        </Card>
        <Card className="bg-white dark:bg-gray-800 shadow-sm hover:shadow-md transition-shadow duration-200 ease-in-out border border-gray-200 dark:border-gray-700">
          <CardHeader className="flex flex-row items-center justify-between space-y-0 pb-2">
            <CardTitle className="text-sm font-medium text-gray-600 dark:text-gray-400">Unique Visitors</CardTitle>
            <svg
              xmlns="http://www.w3.org/2000/svg"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              strokeLinecap="round"
              strokeLinejoin="round"
              strokeWidth="2"
              className="h-5 w-5 text-gray-400 dark:text-gray-500"
              aria-hidden="true"
            >
              <path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2" />
              <circle cx="9" cy="7" r="4" />
              <path d="M22 21v-2a4 4 0 0 0-3-3.87M16 3.13a4 4 0 0 1 0 7.75" />
            </svg>
          </CardHeader>
          <CardContent>
            <div className="text-3xl font-bold text-gray-900 dark:text-gray-50">{totalUniqueVisitors.toLocaleString()}</div>
            <p className="text-xs text-green-600 dark:text-green-400">+18.5% from last month</p>
          </CardContent>
        </Card>
        <Card className="bg-white dark:bg-gray-800 shadow-sm hover:shadow-md transition-shadow duration-200 ease-in-out border border-gray-200 dark:border-gray-700">
          <CardHeader className="flex flex-row items-center justify-between space-y-0 pb-2">
            <CardTitle className="text-sm font-medium text-gray-600 dark:text-gray-400">Bounce Rate</CardTitle>
            <svg
              xmlns="http://www.w3.org/2000/svg"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              strokeLinecap="round"
              strokeLinejoin="round"
              strokeWidth="2"
              className="h-5 w-5 text-gray-400 dark:text-gray-500"
              aria-hidden="true"
            >
              <path d="M22 12h-4l-3 9L9 3l-3 9H2" />
            </svg>
          </CardHeader>
          <CardContent>
            <div className="text-3xl font-bold text-gray-900 dark:text-gray-50">{(averageBounceRate * 100).toFixed(2)}%</div>
            <p className="text-xs text-red-600 dark:text-red-400">-5.2% from last month</p>
          </CardContent>
        </Card>
        <Card className="bg-white dark:bg-gray-800 shadow-sm hover:shadow-md transition-shadow duration-200 ease-in-out border border-gray-200 dark:border-gray-700">
          <CardHeader className="flex flex-row items-center justify-between space-y-0 pb-2">
            <CardTitle className="text-sm font-medium text-gray-600 dark:text-gray-400">Avg. Session Duration</CardTitle>
            <svg
              xmlns="http://www.w3.org/2000/svg"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              strokeLinecap="round"
              strokeLinejoin="round"
              strokeWidth="2"
              className="h-5 w-5 text-gray-400 dark:text-gray-500"
              aria-hidden="true"
            >
              <rect width="7" height="7" x="3" y="3" rx="1" />
              <rect width="7" height="7" x="14" y="3" rx="1" />
              <rect width="7" height="7" x="14" y="14" rx="1" />
              <rect width="7" height="7" x="3" y="14" rx="1" />
            </svg>
          </CardHeader>
          <CardContent>
            <div className="text-3xl font-bold text-gray-900 dark:text-gray-50">{formatDuration(averageSessionDuration)}</div>
            <p className="text-xs text-green-600 dark:text-green-400">+1.1% from last month</p>
          </CardContent>
        </Card>
      </div>

      <Card className="bg-white dark:bg-gray-800 shadow-sm border border-gray-200 dark:border-gray-700">
        <CardHeader>
          <CardTitle className="text-lg font-semibold text-gray-900 dark:text-gray-50">Traffic Data Over Time</CardTitle>
        </CardHeader>
        <CardContent>
          {/* Placeholder for a chart component, e.g., Recharts or Nivo. */}
          {/* In a real app, you'd integrate a charting library here, passing `data` as props. */}
          <div className="h-64 w-full bg-gray-100 dark:bg-gray-700 rounded-lg flex items-center justify-center text-gray-500 dark:text-gray-400 text-sm italic"
               aria-label="Chart showing traffic data over time">
            [Chart Placeholder: Integrate a charting library like Recharts or Nivo here]
          </div>
          <div className="mt-4 text-sm text-gray-600 dark:text-gray-400">
            <p>Data points: {data.map(d => d.date).join(', ')}</p>
            <p>Views: {data.map(d => d.views).join(', ')}</p>
            <p>Unique Visitors: {data.map(d => d.uniqueVisitors).join(', ')}</p>
          </div>
        </CardContent>
      </Card>

      <div className="mt-8 flex justify-end">
        <Button 
          onClick={refetch} 
          disabled={isLoading} 
          className="bg-blue-600 hover:bg-blue-700 dark:bg-blue-700 dark:hover:bg-blue-800 text-white font-semibold py-2 px-4 rounded-md shadow-sm transition-colors duration-200 ease-in-out"
          aria-label="Refresh traffic data"
        >
          <RefreshCw className="mr-2 h-4 w-4" />
          {isLoading ? 'Refreshing...' : 'Refresh Data'}
        </Button>
      </div>
    </div>
  );
};

export default TrafficAnalytics;
