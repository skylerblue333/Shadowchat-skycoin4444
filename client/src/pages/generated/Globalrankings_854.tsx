// AUTO-GENERATED DRAFT SCREEN: GlobalRankings
import React from 'react';
import { useQuery } from '@tanstack/react-query'; // Assuming tRPC integrates with react-query
import { Card, CardContent, CardHeader, CardTitle } from './ui/card'; // shadcn/ui Card component
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from './ui/table'; // shadcn/ui Table component
import { Skeleton } from './ui/skeleton'; // shadcn/ui Skeleton component for loading states
import { Alert, AlertDescription, AlertTitle } from './ui/alert'; // shadcn/ui Alert component for error handling
import { RocketIcon } from '@radix-ui/react-icons'; // Example icon

// Simulate tRPC client and types
// In a real application, these would be generated by tRPC
const trpc = {
  globalRankings: {
    getRankings: {
      useQuery: (options?: any) => useQuery<RankEntry[], Error>(['globalRankings'], async () => {
        // Simulate API call delay
        await new Promise(resolve => setTimeout(resolve, 1000));
        // Simulate data
        if (Math.random() < 0.1) throw new Error('Failed to fetch rankings.');
        return [
          { id: '1', rank: 1, username: 'CryptoKing', score: 150000 },
          { id: '2', rank: 2, username: 'BlockchainQueen', score: 120000 },
          { id: '3', rank: 3, username: 'TokenMaster', score: 100000 },
          { id: '4', rank: 4, username: 'DeFi_Dude', score: 95000 },
          { id: '5', rank: 5, username: 'NFT_Nerd', score: 80000 },
          { id: '6', rank: 6, username: 'HODL_Hero', score: 75000 },
          { id: '7', rank: 7, username: 'Mining_Maniac', score: 60000 },
          { id: '8', rank: 8, username: 'Web3_Wizard', score: 55000 },
          { id: '9', rank: 9, username: 'SmartContractor', score: 40000 },
          { id: '10', rank: 10, username: 'Metaverse_Maven', score: 35000 },
        ];
      }, options),
    },
  },
};

interface RankEntry {
  id: string;
  rank: number;
  username: string;
  score: number;
}

interface GlobalRankingsProps {
  // No specific props for this screen, but can be extended if needed
}

const GlobalRankings: React.FC<GlobalRankingsProps> = () => {
  const { data: rankings, isLoading, isError, error } = trpc.globalRankings.getRankings.useQuery();

  if (isLoading) {
    return (
      <div role="status" aria-live="polite" className="flex flex-col items-center justify-center min-h-screen bg-background p-4 dark:bg-gray-900">
        <Card className="w-full max-w-3xl dark:bg-gray-800 dark:text-gray-50">
          <CardHeader>
            <CardTitle className="text-2xl font-bold dark:text-gray-50">Global Rankings</CardTitle>
          </CardHeader>
          <CardContent>
            <div className="space-y-4">
              {[...Array(10)].map((_, i) => (
                <div key={i} className="flex items-center space-x-4">
                  <Skeleton className="h-8 w-8 rounded-full dark:bg-gray-700" aria-hidden="true" />
                  <Skeleton className="h-8 w-[150px] dark:bg-gray-700" aria-hidden="true" />
                  <Skeleton className="h-8 w-[100px] ml-auto dark:bg-gray-700" aria-hidden="true" />
                </div>
              ))}
            </div>
          </CardContent>
        </Card>
      </div>
    );
  }

  if (isError) {
    return (
      <div className="flex flex-col items-center justify-center min-h-screen bg-background p-4 dark:bg-gray-900">
        <Alert role="alert" aria-live="assertive" variant="destructive" className="w-full max-w-3xl dark:bg-red-900 dark:text-red-50">
          <RocketIcon className="h-4 w-4" aria-hidden="true" />
          <AlertTitle>Error</AlertTitle>
          <AlertDescription>
            Failed to load global rankings: {error?.message || 'Unknown error'}
          </AlertDescription>
        </Alert>
      </div>
    );
  }

  return (
    <div role="main" aria-live="polite" className="flex flex-col items-center justify-center min-h-screen bg-background p-4 dark:bg-gray-900">
      <Card className="w-full max-w-3xl dark:bg-gray-800 dark:text-gray-50">
        <CardHeader>
          <CardTitle className="text-3xl font-extrabold text-center dark:text-gray-50">SKYCOIN4444 Global Rankings</CardTitle>
        </CardHeader>
        <CardContent>
          {rankings && rankings.length > 0 ? (
            <Table className="w-full" summary="Global rankings of players">
              <TableHeader>
                <TableRow className="dark:hover:bg-gray-700">
                  <TableHead className="w-[80px] text-lg font-semibold dark:text-gray-300">Rank</TableHead>
                  <TableHead className="text-lg font-semibold dark:text-gray-300">Username</TableHead>
                  <TableHead className="text-right text-lg font-semibold dark:text-gray-300">Score</TableHead>
                </TableRow>
              </TableHeader>
              <TableBody>
                {rankings.map((entry) => (
                  <TableRow key={entry.id} className="dark:hover:bg-gray-700">
                    <TableCell className="font-medium text-base dark:text-gray-100">{entry.rank}</TableCell>
                    <TableCell className="text-base dark:text-gray-100">{entry.username}</TableCell>
                    <TableCell className="text-right text-base dark:text-gray-100">{entry.score.toLocaleString()}</TableCell>
                  </TableRow>
                ))}
              </TableBody>
            </Table>
          ) : (
            <p className="text-center text-gray-500 dark:text-gray-400">No rankings available.</p>
          )}
        </CardContent>
      </Card>
    </div>
  );
};

export default GlobalRankings;
